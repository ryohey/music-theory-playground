{
  "name": "guitar-scales",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ryohey.github.io/music-theory-playground/",
  "devDependencies": {
    "react-scripts": "1.1.4",
    "shx": "^0.3.2"
  },
  "dependencies": {
    "color": "^3.0.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "shx rm -rf build docs && npm run build && shx cp -r build docs && shx echo Done"
  }
}
